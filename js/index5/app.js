/* // Función para activar y desactivar el hover
function alternarHover() {
    var elemento = document.getElementById('cards-1');
    elemento.classList.toggle('hover-activo');
  }
  
  // Ejecutar la función cada cierto tiempo
  setInterval(alternarHover, 2000);  */