
        let icon_1 = gsap.fromTo(
          '#Icon1',
          { opacity: 0, y: -200, scale: 0 },
          { opacity: 1, y: 0, scale: 1, duration: 1, delay: 2.5 }
        );  
        let icon_5 = gsap.fromTo(
          '#Icon2',
          { opacity: 0, y: -200, scale: 0 },
          { opacity: 1, y: 0, scale: 1, duration: 1, delay: 2 }
        );  
        let icon_2 = gsap.fromTo(
          '#Icon3',
          { opacity: 0, y: -200, scale: 0 },
          { opacity: 1, y: 0, scale: 1, duration: 1, delay: 1.5 }
        ); 
         let icon_3 = gsap.fromTo(
          '#Icon4',
          { opacity: 0, y: -200, scale: 0 },
          { opacity: 1, y: 0, scale: 1, duration: 1, delay: 1 }
        );
        let icon_4 = gsap.fromTo(
          '#Icon5',
          { opacity: 0, y: -200, scale: 0 },
          { opacity: 1, y: 0, scale: 1, duration: 1, delay: .5 }
        );


